export interface Department {
    deptID: number;
    deptName: string;       
    status: boolean;
    archive: boolean;
    sapCode: string;
    groupID: number;
    // createdBy: string;
    // createdOn: string;          
    // modifiedBy: string;
    // modifiedOn: string;        
}
